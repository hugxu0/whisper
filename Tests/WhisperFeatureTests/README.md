# WhisperFeatureTests

Feature tests will exercise state/action/effect transitions without rendering
the iOS UI. The first suite will cover session launch, login success, bootstrap
success, socket connection state, retry, and logout cleanup.
