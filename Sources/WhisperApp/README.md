# WhisperApp composition root

The iOS application target will be added on the macOS/Xcode path after the
fixture-backed session slice is stable. This directory is reserved for app
composition, dependency injection, and the SwiftUI entry point; feature code
must not move into it.
