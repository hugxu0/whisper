# WhisperDesignSystem

This module will own new visual tokens and reusable components. It is not a
wrapper around the legacy app's colors, fonts, or view hierarchy. Visual
references are captured as product decisions before components are implemented.
